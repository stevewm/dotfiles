# terraform
if type -q terraform
    abbr tf 'terraform'
    alias tfa 'terraform apply'
    alias tfp 'terraform plan'
    alias tfi 'terraform init'
    alias tfc 'open https://app.terraform.io/'
end
